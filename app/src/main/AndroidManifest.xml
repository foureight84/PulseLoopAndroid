<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Phase 2: BLE permissions -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true"/>

    <!-- Android 12+ BLE permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Location permission required for BLE scanning on Android < 12 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Coach weather context (iOS #65d) only needs city-level precision; declared explicitly
         even though ACCESS_FINE_LOCATION above already implies it. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Phase 7: Notifications for coach check-ins (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Phase 4+: Foreground service for workout. Android 14+ requires the "health" foreground
         service type to be paired with at least one of ACTIVITY_RECOGNITION/BODY_SENSORS/
         HIGH_SAMPLING_RATE_SENSORS or startForeground() throws SecurityException — this was
         missing entirely, so every workout start crashed on API 34+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Self-update: fetch the GitHub release manifest/APK and install it -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Phase 8: Health Connect export (write-only). Exactly the Phase 1–4 data types from
         docs/health-connect-integration.md — Phase 5 types (blood pressure, glucose, respiratory
         rate, VO2max, resting HR, nutrition) join the manifest when that phase lands, so the
         first permission sheet stays narrow. No READ_* permissions anywhere. -->
    <uses-permission android:name="android.permission.health.WRITE_HEART_RATE" />
    <uses-permission android:name="android.permission.health.WRITE_OXYGEN_SATURATION" />
    <uses-permission android:name="android.permission.health.WRITE_HEART_RATE_VARIABILITY" />
    <uses-permission android:name="android.permission.health.WRITE_BODY_TEMPERATURE" />
    <uses-permission android:name="android.permission.health.WRITE_SLEEP" />
    <uses-permission android:name="android.permission.health.WRITE_STEPS" />
    <uses-permission android:name="android.permission.health.WRITE_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.WRITE_DISTANCE" />
    <uses-permission android:name="android.permission.health.WRITE_EXERCISE" />
    <!-- Singular on purpose: the official docs note WRITE_EXERCISE_ROUTE is singular while
         READ_EXERCISE_ROUTES is plural. -->
    <uses-permission android:name="android.permission.health.WRITE_EXERCISE_ROUTE" />

    <application
        android:name=".PulseLoopApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Strava OAuth redirect: captures pulseloop://localhost/strava-auth?code=... -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="pulseloop" android:host="localhost" android:pathPrefix="/strava-auth" />
            </intent-filter>
        </activity>

        <!-- FileProvider for diagnostics export -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Phase 6: Workout foreground service -->
        <service
            android:name=".service.WorkoutForegroundService"
            android:foregroundServiceType="health"
            android:exported="false" />

        <!-- Home-screen widgets (iOS #44): three Glance app widgets + the shared configure
             activity for the two configurable ones. -->
        <receiver
            android:name=".widgets.PulseActivityWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_activity_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_activity_info" />
        </receiver>
        <receiver
            android:name=".widgets.PulseMetricWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_metric_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_metric_info" />
        </receiver>
        <receiver
            android:name=".widgets.PulseDualMetricWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_dual_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_dual_info" />
        </receiver>
        <activity
            android:name=".widgets.WidgetConfigActivity"
            android:exported="true"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Phase 8: Health Connect rationale — target of the "privacy policy" link in the
             permission sheet, mandatory for apps requesting health data types (official
             get-started guide). Pre-34 fires ACTION_SHOW_PERMISSIONS_RATIONALE at the activity;
             API 34+ reaches the alias via VIEW_PERMISSION_USAGE + HEALTH_PERMISSIONS, guarded
             by START_VIEW_PERMISSION_USAGE. In-app rationale only, no hosted URL (Gadgetbridge
             precedent). -->
        <activity
            android:name=".health.HealthConnectRationaleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>
        <activity-alias
            android:name=".health.ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".health.HealthConnectRationaleActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE"
            tools:targetApi="34">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>
    </application>

    <!-- Health Connect: provider app (the separate APK on API < 34) plus the rationale action,
         so the permission sheet's privacy-policy link resolves to our activity. -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
    </queries>
</manifest>
